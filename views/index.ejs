<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="img2.png" type="image/png">
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="newfile.css">
    
    <title>Image to Text</title>
</head>
<!--  -->
<body>
    <nav>
        <div class="container">
            <a href="/">
                <h1 id="heading-of-container">OCR In Image
            </a>
        </div>
    </nav>
    <div class="main-container">
        <!-- the modules which are added are basically works upon the forms -->
        <!--  NOTE: here the reason of using post is only because we send some pics that means we are using post request to post some data on this -->
        <form action="/upload" method="POST" enctype="multipart/form-data">
            <div class="drag-icon"><i class="fas fa-cloud-upload-alt"></i></div>
            <header>Drag & Drop a File</header>
            <span>Or</span>
            <div class="input-item">
                 <input type="file" id="choose-file" 
                    name="avatar" />
            </div>
            <div class="convert-btn">
                <button type="submit">Convert</button>
            </div>
        </form>
    </div>
    <p class="copyright">&copy;singhkunal01</p>
<script >
    const submitBtn = document.querySelector('.convert-btn button');
    submitBtn.addEventListener('click',checkIfButtonClicked);
    function checkIfButtonClicked(){
        if(document.getElementById('choose-file').value=='' ) {
            alert('Please Upload Something.');
         }
         return;
    }
</script>
</body>
</html>